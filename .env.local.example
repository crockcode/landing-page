# Supabase credentials (required for waitlist functionality)
# Replace these values with your actual Supabase credentials from your project dashboard
# Your URL should look like: https://abcdefghijklm.supabase.co (no trailing slash)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# For server-side operations (keep these secure, never expose to client)
# This is your service role key from the same API settings page
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Secure admin key for API access to waitlist data
# Generate a strong random string, e.g.: openssl rand -base64 32
WAITLIST_ADMIN_KEY=your-random-secure-key-for-admin-access 